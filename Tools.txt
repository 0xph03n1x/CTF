#!/bin/bash

Starting nessus:

sudo /bin/systemctl start nessusd.service

Xfreerdp usage:

xfreerdp /f /u:USERNAME /p:PASSWRD /v:HOST

IDA freeware only local x86/x64.

Pepend HTB and put brackets on flag:

var=$(cat flag.txt) && echo HTB{$var}
HTB: sudo route delete default gw 10.10.14.1
THM: sudo route delete default gw 10.18.0.1 

For steg challenges use steghide from github(usage - jave -jar steghide.jar)


Burp Suite:

Ctrl + Shift + D	Switch to the Dashboard
Ctrl + Shift + T	Switch to the Target tab
Ctrl + Shift + P	Switch to the Proxy tab
Ctrl + Shift + I	Switch to the Intruder tab
Ctrl + Shift + R	Switch to the Repeater tab
